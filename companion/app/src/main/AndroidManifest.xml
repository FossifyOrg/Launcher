<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="LAUNCHPAD Eltern"
        android:theme="@style/Theme.MaterialComponents.DayNight.DarkActionBar"
        android:allowBackup="false">

        <activity
            android:name=".CompanionActivity"
            android:exported="true"
            android:label="LAUNCHPAD Eltern">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Eltern widget: shows pending request counts on the parent's home screen -->
        <receiver
            android:name=".CompanionWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/companion_widget_info" />
        </receiver>

    </application>
</manifest>
