// LAUNCHPAD Companion is a STANDALONE Gradle project; the active build file is:
//   companion/app/build.gradle.kts
// Build from the repo root:
//   ./build-companion.sh
// or equivalently:
//   ./gradlew -p companion :app:assembleDebug
//
// This file stays empty so that ./gradlew -p companion succeeds (companion/settings.gradle.kts
// only includes :app, so this root project has no plugins to apply).
